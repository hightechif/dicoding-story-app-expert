<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.StoryApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.StoryApp" parent="Base.Theme.StoryApp" />

    <style name="HomeTheme" parent="Theme.Material3.DayNight" />

    <!-- Splash screen theme   -->
    <style name="SplashTheme" parent="Theme.StoryApp">
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryDarkColor</item>
        <item name="colorAccent">@color/primaryColorVariant</item>
        <item name="android:statusBarColor">@color/primaryColorVariant</item>
    </style>
</resources>